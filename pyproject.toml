[tool.poetry]
name = "hello-python"
version = "0.1.0"
description = "A simple Python project that prints 'Hello, World!'"
authors = ["Your Name <youremail@example.com>"]
license = "MIT"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"